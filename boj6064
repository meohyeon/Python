case = int(input())
boolean = True

for i in range(case):
    m, n, x, y = map(int,input().split())
    boolean = True
    while x<=m*n:
        if (x-y)%n==0:
            print(x)
            boolean=False
            break
        x += m
        
    if boolean:
        print(-1)